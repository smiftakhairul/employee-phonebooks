</div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <?php if (isset($isForm) && $isForm) { ?><button type="submit" name="submit" class="btn btn-primary">Submit</button><?php } ?>
      </div>
      <?php if (isset($isForm) && $isForm) { ?></form><?php } ?>
    </div>
  </div>
</div>